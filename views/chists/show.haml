%h2="Chist: #{chist.title}"

%div
  =chist.chist

%p=chist.public ? I18n.t('chists.public_chist') : I18n.t('chists.private_chist')

%p=chist.format

- if current_user && current_user.id == chist.user_id
  %a{href: "/chists/#{chist.id}/edit"}=I18n.t('chists.edit')
