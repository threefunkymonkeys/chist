<!DOCTYPE html>
%html{ :lang => :en }
  %head
    - if defined?(chist)
      -# Open Graph
      %meta{ name: "og:type", content:"article"}
      %meta{ name: "og:title", content:"Chist | #{chist.title}"}
      %meta{ name: "og:description", content:"#{chist.chist.lines[0..4].join("\n")}"}
      %meta{ name: "og:url", content:"#{ENV['SITE_URL']}/chists/#{chist.id}"}
      %meta{ name: "og:site_name", content:"Chist"}
      %meta{ name: "twitter:card", content: "summary"}
      %meta{ name: "twitter:site", content:"iChist"}
      %meta{ name: "twitter:title", content:"Chist | #{chist.title}"}
      %meta{ name: "twitter:description", content:"#{chist.chist.lines[0..4].join("\n")}"}
      %meta{ name: "twitter:creator", content:"@3funkymonkeys"}
      %meta{ name: "twitter:image:src", content:"#{ENV['SITE_URL']}/img/favicon.ico"}
      %meta{ name: "twitter:domain", content: "ichist.com"}
      %link{ :rel => "canonical", :href => "#{ENV['SITE_URL']}/chists/#{chist.id}" }
      %link{ :rel => "alternate", :type => "application/json+oembed", :href => "#{ENV['SITE_URL']}/services/oembed?url=#{URI.encode_www_form_component("#{ENV['SITE_URL']}/chists/#{chist.id}")}&format=json", :title => "#{chist.user.name}/#{chist.title}"}
      -# Twitter Card
    %title Chist
    %meta{ :name => "viewport", :content => "width=device-width, initial-scale=1.0" }
    - if defined?(is_public) && !is_public
      %meta{ :name => "robots", :content => "noindex, nofollow"}
      %meta{ :name => "referrer", :content => "never"}
    %link{ :href => "/img/favicon.ico", :rel => "shortcut icon"}
    %link{ :href => "/css/bootstrap.css", :rel => "stylesheet" }
    %link{ :href => "/css/chosen.min.css", :rel => "stylesheet" }
    %link{ :href => "/css/chosen_select_extend.css", :rel => "stylesheet" }
    /%link{ :href => "/css/layout.css", :rel => "stylesheet" }
    %link{ :href => "/css/chist.css", :rel => "stylesheet" }
    /%link{ :href => "/css/base.css", :rel => "stylesheet" }
    /%link{ :href => "/css/bootstrap_override.css", :rel => "stylesheet" }
    %link{ :href => 'http://fonts.googleapis.com/css?family=Oxygen+Mono', :rel => 'stylesheet', :type => 'text/css' }
  %body
    #wrap
      = partial 'shared/_header'
      #container.container
        .flash-messages
          =show_flash_message
        = yield
      = partial 'shared/_footer'
      = partial 'shared/_delete_chist_modal'

    %script{ :src => "https://ajax.googleapis.com/ajax/libs/jquery/1.11.0/jquery.min.js", :type => "text/javascript" }
    %script{ :src => "//netdna.bootstrapcdn.com/bootstrap/3.1.1/js/bootstrap.min.js" }
    %script{ :src => "/js/chosen.jquery.min.js", :type => "text/javascript" }
    %script{ :src => "/js/chist.js", :type => "text/javascript" }
